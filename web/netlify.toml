[build]
  base = "web"
  publish = "build"
  command = "rm -rf node_modules package-lock.json && npm install --production --legacy-peer-deps --force && npm run build"

[build.environment]
  NODE_VERSION = "20" 