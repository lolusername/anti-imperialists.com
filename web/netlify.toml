# Disable all caching to fix dependency conflicts
[build]
  base = "web"
  publish = "build"
  command = "npm install --legacy-peer-deps --force && npm run build"

[build.environment]
  NODE_VERSION = "20"
  NPM_CONFIG_CACHE = "/tmp/.npm"
  NETLIFY_CACHE_SKIP = "true" 